apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ring-mqtt.fullname" . }}-test-connection"
  labels:
    {{- include "ring-mqtt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ring-mqtt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "ring-mqtt.fullname" . }}
  labels:
    {{- include "ring-mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ring-mqtt.selectorLabels" . | nindent 4 }}
